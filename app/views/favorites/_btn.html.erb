<% if post.favorites.exists?(user_id: current_user.id) %>
  <%= button_to "❤️", post_favorite_path(post), method: :delete %>
<% else %>
  <%= button_to "♡", post_favorite_path(post), method: :post %>
<% end %>