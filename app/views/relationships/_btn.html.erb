<% if current_user.following?(user) %>
  <%= button_to "フォロー外す", user_relationships_path(user), method: :delete, class: "btn btn-outline-secondary btn-sm" %>
<% else %>
  <%= button_to "フォローする", user_relationships_path(user), method: :post, class: "btn btn-outline-secondary btn-sm" %>
<% end %>