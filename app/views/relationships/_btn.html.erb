<% if current_user.following?(user) %>
  <%= button_to "フォロー外す", user_relationships_path(user), method: :delete, class: "btn-danger" %>
<% else %>
  <%= button_to "フォローする", user_relationships_path(user), method: :post, class: "btn-primary" %>
<% end %>